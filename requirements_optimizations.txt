# Требования для системы оптимизации производительности

# Основные зависимости
aiomysql>=0.2.0          # Асинхронный MySQL клиент для connection pooling
redis>=4.5.0             # Redis клиент для кэширования и rate limiting
psutil>=5.9.0            # Системная статистика для мониторинга
aiofiles>=22.1.0         # Асинхронная работа с файлами для бэкапов
aiohttp>=3.8.0           # HTTP клиент для облачного хранилища
prometheus-client>=0.15.0 # Prometheus метрики

# Дополнительные зависимости для расширенной функциональности
asyncpg>=0.27.0          # Альтернативный PostgreSQL клиент (опционально)
aioredis>=2.0.0          # Альтернативный Redis клиент (опционально)
pyyaml>=6.0              # YAML конфигурации (опционально)
python-dotenv>=0.20.0    # Environment variables (опционально)

# Тестирование
pytest>=7.0.0            # Тестирование
pytest-asyncio>=0.19.0   # Асинхронное тестирование
pytest-cov>=4.0.0        # Покрытие кода тестами
pytest-mock>=3.7.0       # Моки для тестирования

# Мониторинг и логирование
structlog>=21.5.0        # Структурированное логирование
colorlog>=6.6.0          # Цветное логирование
watchdog>=2.1.0          # Файловый мониторинг (опционально)

# Инструменты разработки
black>=22.0.0            # Форматирование кода
isort>=5.10.0            # Сортировка импортов
flake8>=5.0.0            # Линтинг кода
mypy>=0.950              # Типизация

# Производительность
uvloop>=0.16.0           # Высокопроизводительный event loop (опционально)
orjson>=3.7.0            # Быстрый JSON парсер (опционально)
ujson>=5.1.0             # Альтернативный JSON парсер (опционально)

# Документация
sphinx>=5.0.0            # Генерация документации (опционально)
sphinx-rtd-theme>=1.0.0  # Тема для Sphinx (опционально)